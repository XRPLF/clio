if(CMAKE_CXX_COMPILER_ID STREQUAL "Clang")
  message(STATUS "Enabling thread sanitizer")
  target_compile_options(clio_tests PRIVATE -g -fsanitize=thread) 
  target_link_options (clio_tests PRIVATE -g -fsanitize=thread)
  target_compile_options(clio_server PRIVATE -g -fsanitize=thread) 
  target_link_options (clio_server PRIVATE -g -fsanitize=thread)
else()
  message(FATAL "ENABLE_TSAN requested but compiler is not Clang")
endif()
